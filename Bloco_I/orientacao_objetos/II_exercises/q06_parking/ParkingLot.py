# Classe ParkingLot :
# Atributos: total_spots (número total de vagas), occupied_spots (número atual de vagas ocupadas).
# Métodos:
# vehicle_entry() : Adiciona um veículo, verificando se há vagas disponíveis. Caso o estacionamento esteja cheio, lance uma exceção
# ParkingLotFullError .
# vehicle_exit() : Remove um veículo, decrementando o número de vagas ocupadas. Caso o estacionamento esteja vazio, lance uma
# exceção ParkingLotEmptyError .
# show_status() : Exibe o número de vagas disponíveis e ocupadas.
